[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
# Pass environment variables as build arguments
buildArgs = ["VITE_API_BASE_URL"]

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
